SCRIPT_VERSION=v1.2.0

# Users and groups
UID=${UID}
GID=${GID}
DOCKER_GID=${DOCKER_GID}

# Proxy settings
no_proxy=${no_proxy:-}
http_proxy=${http_proxy:-}
https_proxy=${https_proxy:-}
NO_PROXY=${NO_PROXY:-}
HTTP_PROXY=${HTTP_PROXY:-}
HTTPS_PROXY=${HTTPS_PROXY:-}
